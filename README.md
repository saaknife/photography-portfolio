# 摄影作品集

一个简单的摄影作品展示页面，直接用浏览器打开 `index.html` 即可浏览。

## 页面功能

- 全屏首屏展示
- 瀑布流作品列表
- 按竖幅、横幅、方形筛选
- 点击作品查看大图，支持键盘与触摸切换

## 使用说明

作品图片存放在 `图片/` 文件夹中，网页使用的压缩版本位于 `images/` 文件夹。新增作品时，可以参考 `index.html` 中的作品卡片结构，替换图片路径、标题和画面信息。
